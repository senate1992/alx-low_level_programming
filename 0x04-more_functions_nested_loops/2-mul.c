include "main.h"

/**
 * mul - multiply two integers
 * @x: first integer to multiply
 * @y: second integer to multiply with first integer
 * Return: The reult of multiplication between x param and y param
 */

int mul(int x, int y)

{
	return (x * y);
}
